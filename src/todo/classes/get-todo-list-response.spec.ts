import { GetTodoListResponse } from './get-todo-list-response';

describe('GetTodoListResponseDto', () => {
  it('should be defined', () => {
    expect(new GetTodoListResponse()).toBeDefined();
  });
});
